projection implementation in class ZBP_C_ARTICLE unique;
strict ( 2 );
extensible;
use draft;
use side effects;
define behavior for ZC_ARTICLE
extensible
use etag

{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
}